# Pact files are generated by the consumer test suite.
# They are committed here for file-based sharing (no Pact Broker required).
# See docs/pact-setup.md for the full workflow.
#
# To regenerate:
#   cd frontend && npm run test:pact
